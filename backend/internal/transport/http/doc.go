// Package http hosts the HTTP handlers, routing, and middleware for the
// backend transport layer.
//
// It is currently an empty skeleton created by the backend scaffold; the
// first real route (GET /healthz) is registered in cmd/server until the
// first domain task moves handler wiring here.
package http
