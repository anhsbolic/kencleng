// Placeholder — real form is Account Task #4's scope
// (docs/spec/1-account/tasks.md), not this playbook
// (phase0-shared-infra.md). Exists so the Auth Shell has a route to
// render against during this phase's verification.
export default function ResetPasswordPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-neutral-900">Reset Password</h1>
      <p className="mt-2 text-sm text-neutral-500">Placeholder — Account Task #4.</p>
    </div>
  );
}
