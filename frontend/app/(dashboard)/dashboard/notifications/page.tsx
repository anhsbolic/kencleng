// Placeholder — real list view is `notification` domain scope
// (docs/ui-ux/page-map.md), not this playbook
// (phase0-shared-infra.md). Exists so the Dashboard Shell has a
// route to render against during this phase's verification.
export default function NotificationsPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-neutral-900">Notifikasi</h1>
      <p className="mt-2 text-sm text-neutral-500">Placeholder — notification domain.</p>
    </div>
  );
}
