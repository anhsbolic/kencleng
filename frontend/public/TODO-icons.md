# TODO — PWA icon assets

`manifest.json` references `/icon-192.png` and `/icon-512.png`, which
don't exist yet. Generating them is a design-asset task, not part of
`scaffold-frontend.md` (Step 5) — this file is the visible placeholder
that playbook calls for so the gap isn't silently missed.

Delete this file once both icons are added to `public/`.
